const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const notes = await prisma.clinicalNote.findMany(); const apts = await prisma.appointment.findMany({select:{id:true}}); const aptIds = apts.map(a=>a.id); const orphaned = notes.filter(n=>!aptIds.includes(n.appointmentId)); console.log(JSON.stringify(orphaned, null, 2)); } run().catch(console.error).finally(() => prisma.$disconnect());
